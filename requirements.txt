streamlit
openai>=1.0.0          # if using OpenAI (or remove if not)
groq                   # if using Groq (very fast & has free tier)
anthropic              # if using Claude
langchain              # optional — if you want chaining / memory
python-dotenv          # if using .env for API keys (recommended!)
llama-3.3-70b-versatile
